sudo: required
services:
    - docker

before install:
    - docker build -t nikhilck/docker-react -f Dockerfile.dev .

script:
    - docker run -e CI=true nikhilck/docker-react npm run test -- --coverage
